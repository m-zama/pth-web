<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.stylez.zama.dao.TestDao">
  <select id="selectTestAccountListSql" resultType="TestDto">
    SELECT USER_ID as userId, LOGIN_ID as loginId, PASSWORD as password, USER_NAME as userName FROM TEST_USER
  </select>

  <select id="selectTestAccountByLoginIdSql" resultType="TestDto" parameterType="string">
    SELECT user_id AS userId, login_id AS loginId, password AS password, user_name AS userName FROM test_user WHERE login_id = #{loginId}
  </select>
</mapper>